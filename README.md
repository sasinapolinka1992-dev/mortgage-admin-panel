# Инструкция по запуску проекта Mortgage Admin Panel

## 📋 Что это за проект?

Это веб-приложение для управления банками и ипотечными программами. Оно позволяет добавлять банки, настраивать ипотечные программы и управлять их видимостью.

---

## 🚀 Быстрый старт (для новичков)

### Шаг 1: Установка Node.js

**Если у вас еще не установлен Node.js:**

1. В папке с проектом найдите файл `node-v24.12.0-x64.msi`
2. Дважды кликните на этот файл, чтобы запустить установщик
3. Следуйте инструкциям установщика:
   - Нажимайте "Next" (Далее) на каждом шаге
   - Примите условия лицензии
   - Оставьте все настройки по умолчанию
   - Нажмите "Install" (Установить)
   - Дождитесь завершения установки
   - Нажмите "Finish" (Готово)

**Проверка установки:**
- Нажмите клавиши `Win + R` (Windows) или откройте меню "Пуск"
- Введите `cmd` и нажмите Enter (откроется черное окно - это командная строка)
- В командной строке введите: `node --version`
- Нажмите Enter
- Должно появиться что-то вроде: `v24.12.0`

Если версия отображается - Node.js установлен правильно! ✅

---

### Шаг 2: Открытие проекта в командной строке

1. Откройте папку с проектом `mortgage-admin-panel` в проводнике Windows
2. В адресной строке проводника (где показан путь к папке) кликните один раз
3. Путь выделится - скопируйте его (Ctrl+C) или запомните
4. Откройте командную строку (как в шаге 1)
5. Введите команду: `cd ` (с пробелом в конце)
6. Вставьте скопированный путь (Ctrl+V) или введите его вручную
7. Нажмите Enter

**Пример:**
```
cd C:\Users\molotkov.av\Downloads\Разработка\Polink\mortgage-admin-panel
```

Теперь вы находитесь в папке проекта! ✅

---

### Шаг 3: Установка зависимостей проекта

В той же командной строке (где вы уже находитесь в папке проекта):

1. Введите команду: `npm install`
2. Нажмите Enter
3. Подождите, пока процесс завершится (может занять 1-3 минуты)
4. Вы увидите много текста - это нормально
5. Когда появится строка с вашим именем пользователя (например, `C:\Users\...>`), значит установка завершена ✅

**Что происходит:** Скачиваются и устанавливаются все необходимые библиотеки для работы проекта.

---

### Шаг 4: Запуск проекта

В той же командной строке:

1. Введите команду: `npm run dev`
2. Нажмите Enter
3. Подождите несколько секунд
4. Вы увидите сообщение примерно такое:
   ```
   VITE v5.x.x  ready in xxx ms

   ➜  Local:   http://localhost:5173/
   ➜  Network: use --host to expose
   ```

**Важно:** Не закрывайте это окно командной строки! Оно должно оставаться открытым.

---

### Шаг 5: Открытие приложения в браузере

1. Откройте любой браузер (Chrome, Edge, Firefox и т.д.)
2. В адресной строке браузера введите: `http://localhost:5173`
3. Нажмите Enter
4. Откроется страница с интерфейсом управления банками ✅

**Готово!** Проект запущен и работает!

---

## 📝 Полезные команды

Все команды нужно выполнять в командной строке, находясь в папке проекта:

| Команда | Что делает |
|---------|------------|
| `npm install` | Устанавливает все необходимые библиотеки (нужно сделать один раз) |
| `npm run dev` | Запускает проект для разработки (открывается в браузере) |
| `npm run build` | Собирает проект для публикации в интернете |
| `npm run preview` | Показывает, как будет выглядеть собранный проект |

---

## 🐛 Решение проблем

### Проблема: "node не является внутренней или внешней командой"

**Что это значит:** Node.js не установлен или не добавлен в PATH.

**Решение:**
1. Убедитесь, что вы установили Node.js (см. Шаг 1)
2. Перезагрузите компьютер после установки
3. Откройте командную строку заново
4. Попробуйте снова: `node --version`

Если не помогло:
- Переустановите Node.js
- При установке убедитесь, что стоит галочка "Add to PATH"

---

### Проблема: "npm не является внутренней или внешней командой"

**Что это значит:** npm не найден (обычно устанавливается вместе с Node.js).

**Решение:**
1. Переустановите Node.js (см. Шаг 1)
2. Перезагрузите компьютер
3. Откройте командную строку заново

---

### Проблема: "localhost отказано в подключении"

**Что это значит:** Сервер не запущен или порт занят.

**Решение:**
1. Убедитесь, что вы выполнили команду `npm run dev` и она работает
2. Проверьте, что окно командной строки открыто и не показывает ошибок
3. Попробуйте закрыть другие программы, которые могут использовать порт 5173
4. Если в консоли указан другой порт (например, 5174), используйте его в браузере

---

### Проблема: Ошибки при установке зависимостей

**Решение:**
1. Убедитесь, что у вас установлен Node.js версии 16 или выше
2. Проверьте подключение к интернету
3. Попробуйте удалить папку `node_modules` и файл `package-lock.json` (если они есть)
4. Запустите `npm install` снова

**Как удалить папку и файл:**
- В проводнике Windows найдите папку `node_modules` в проекте
- Удалите её (Shift+Delete для полного удаления)
- Найдите файл `package-lock.json` и удалите его
- Запустите `npm install` заново

---

### Проблема: Порт 5173 занят

**Решение:**
Vite автоматически выберет свободный порт. Смотрите в консоли, какой порт указан после запуска `npm run dev`, и используйте его в браузере.

---

## 🌐 Публикация проекта в интернете (GitHub Pages)

Если вы хотите разместить проект в интернете, чтобы он был доступен по ссылке:

### Вариант 1: Автоматическая публикация через GitHub Actions (рекомендуется)

#### Шаг 1: Создание репозитория на GitHub

1. Зарегистрируйтесь на [GitHub.com](https://github.com) (если еще не зарегистрированы)
2. Нажмите кнопку "+" в правом верхнем углу
3. Выберите "New repository"
4. Введите название: `mortgage-admin-panel`
5. Выберите "Public" (публичный) или "Private" (приватный)
6. **НЕ** ставьте галочки на "Add a README file" и других опциях
7. Нажмите "Create repository"

#### Шаг 2: Отправка кода на GitHub

**Вариант А: Через веб-интерфейс GitHub (проще для новичков)**

1. После создания репозитория вы увидите страницу с инструкциями
2. Найдите кнопку **"uploading an existing file"** (загрузка существующего файла) или просто перейдите на вкладку **"Code"** в вашем репозитории
3. Нажмите кнопку **"Add file"** (Добавить файл) в правом верхнем углу
4. Выберите **"Upload files"** (Загрузить файлы)
5. Откройте папку с проектом `mortgage-admin-panel` на вашем компьютере
6. Выделите все файлы и папки проекта (кроме папки `node_modules`, если она есть - её загружать не нужно)
7. Перетащите файлы в окно браузера на GitHub или нажмите "choose your files" (выбрать файлы)
8. Внизу страницы введите сообщение: `Initial commit`
9. Нажмите кнопку **"Commit changes"** (Сохранить изменения)
10. Дождитесь завершения загрузки

**Вариант Б: Через командную строку (для продвинутых пользователей)**

В командной строке (в папке проекта):

1. Введите: `git init` и нажмите Enter
2. Введите: `git add .` и нажмите Enter
3. Введите: `git commit -m "Initial commit"` и нажмите Enter
4. Введите: `git branch -M main` и нажмите Enter
5. Введите (замените ВАШ_USERNAME на ваш логин GitHub):
   ```
   git remote add origin https://github.com/ВАШ_USERNAME/mortgage-admin-panel.git
   ```
6. Введите: `git push -u origin main` и нажмите Enter
7. Вас попросят ввести логин и пароль GitHub (или токен доступа)

> **Совет:** Если вы новичок, используйте Вариант А - он проще и не требует знания командной строки.

#### Шаг 3: Настройка GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Нажмите на вкладку **Settings** (вверху страницы)
3. В левом меню найдите раздел **Pages** (в секции "Code and automation")
4. В разделе **Source** выберите **GitHub Actions** (не "Deploy from a branch")
5. Сохраните изменения

#### Шаг 4: Ожидание деплоя

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Вы увидите процесс "Deploy to GitHub Pages"
3. Подождите 1-3 минуты, пока процесс завершится
4. Когда появится зеленая галочка ✅ - деплой успешен!

#### Шаг 5: Доступ к сайту

Ваш сайт будет доступен по адресу:
```
https://ВАШ_USERNAME.github.io/mortgage-admin-panel/
```

Замените `ВАШ_USERNAME` на ваш логин GitHub.

> **Примечание:** После первого деплоя сайт может появиться через несколько минут (до 10 минут).

---

## 📁 Структура проекта

```
mortgage-admin-panel/
├── node-v24.12.0-x64.msi    # Установщик Node.js (если нужен)
├── components/                # Компоненты интерфейса
├── services/                  # Данные и сервисы
├── src/                       # Основной код приложения
├── index.html                 # Главная страница
├── package.json              # Настройки проекта
└── README.md                  # Этот файл
```

---

## ❓ Часто задаваемые вопросы

### Нужно ли устанавливать Node.js каждый раз?

Нет, достаточно установить один раз. Node.js устанавливается на компьютер и работает для всех проектов.

### Можно ли закрыть командную строку после запуска?

Нет, окно командной строки должно оставаться открытым, пока вы работаете с проектом. Если закрыть - проект перестанет работать.

### Как остановить проект?

В окне командной строки нажмите `Ctrl + C`. Проект остановится.

### Как запустить проект снова?

Просто выполните команду `npm run dev` в командной строке (в папке проекта).

### Нужен ли интернет для работы проекта?

- **Для установки:** Да, нужен интернет для скачивания библиотек
- **Для работы:** Нет, после установки проект работает локально без интернета

---

## 🆘 Если ничего не помогает

1. Убедитесь, что Node.js установлен: `node --version` в командной строке
2. Убедитесь, что вы находитесь в правильной папке проекта
3. Попробуйте переустановить зависимости: удалите `node_modules` и `package-lock.json`, затем `npm install`
4. Проверьте версию Node.js (должна быть 16 или выше): `node --version`

---

## 📞 Дополнительная информация

- **Версия Node.js для проекта:** 16 или выше (рекомендуется 24.12.0)
- **Порт по умолчанию:** 5173
- **Технологии:** React, TypeScript, Vite, Tailwind CSS

---
